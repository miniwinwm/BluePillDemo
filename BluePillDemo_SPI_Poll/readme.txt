This example shows how to use the SPI peripheral in polling mode. The SPI's MOSI and
MISO line are connected to each other on the same SPI peripheral so when a read/write
transfer is performed in software the bytes sent are also received.

Schematic available.