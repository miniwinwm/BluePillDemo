The STM32F103C8 processor on the Blue Pill board does not have an AES implementation as
a hardware preipheral so this example implements the AES256 symmetric cryptographic 
algorithm in software.

No schematic file for this example. 